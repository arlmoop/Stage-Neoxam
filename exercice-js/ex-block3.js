console.log(sum(10, 20));

function sum(a, b) { return a+b; }

function isAdult (age) {
    if (age >= 18) {
        return true;
    } else {
        return false;
    }
}

console.log(variable);
var variable = 10;